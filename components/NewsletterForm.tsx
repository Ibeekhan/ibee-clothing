"use client"; import { useState } from "react"; import { Button } from "@/components/ui/Button"; export function NewsletterForm() { const [submitted, setSubmitted] = useState(false); function handleSubmit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); setSubmitted(true); } if (submitted) { return ( <p className="text-sm text-black/70">Thanks — you're on the list.</p> ); } return ( <form className="flex gap-2" onSubmit={handleSubmit}> <input type="email" required placeholder="you@email.com" className="flex-1 border border-[var(--color-line)] bg-white px-4 py-3 text-sm focus:outline-none focus:ring-1 focus:ring-[var(--color-ink)]" /> <Button type="submit">Join</Button> </form> ); }